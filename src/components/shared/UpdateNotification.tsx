import React from 'react'
import UpdateIndicator from '../UpdateIndicator'

/** Backward-compatible update control for surfaces outside the title bar. */
const UpdateNotification: React.FC = () => <UpdateIndicator />

export default UpdateNotification
